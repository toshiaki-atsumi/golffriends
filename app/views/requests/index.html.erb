<h1>入会審査</h1>
<p>idクリックして、入会審査を行って下さい</p>
<table class="table table-striped">
    <thead>
      <tr>
        <th>id</th>
        <th>会の名前</th>
        <th>申請者名</th>
        <th>申請理由</th>
        <th>申請日時</th>
        <th>申請状況</th>
        <th>審査日時</th>       
      </tr>
    </thead>
    <tbody>
     <% @parties.each do |party| %>  
         <% Request.where(party_id: party.id).each do |request| %>
            <% if request.status == '申請中' %>   
              <tr>
                <td><%= link_to request.id,edit_request_path(request.id) %></td>
                <td><%= party.name %></td>
                <td><%= Member.find(request.member_id).name %></td>
                <td><%= request.content %></td>
                <td><%= request.created_at.to_date %></td>
                <td><%= request.status %></td>
                <td><%= request.updated_at.to_date  %></td>
              </tr>
            <% else %>
            <% end %>
          <% end %>
      <% end %>
    </tbody>
  </table>
